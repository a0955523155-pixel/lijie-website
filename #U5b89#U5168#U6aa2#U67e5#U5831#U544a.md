# 俐姐的家｜安全檢查報告

檢查日期：2026-09-07

## 新版本已處理

- 已排除合約、SOP、Excel、Word、PDF 及非網站文件。
- 已使用乾淨的遠端主分支內容，未帶入原壓縮檔中的 Git 合併衝突。
- Firebase Authentication 負責信箱與密碼驗證，網站不保存明文密碼。
- 後台登入後仍會檢查 Firestore `admins/{UID}`；只有登入成功並在名單內的帳號才能管理。
- Firestore Rules 在資料庫端保護草稿、發布內容與版本紀錄，不能只靠前端隱藏按鈕繞過。
- Storage Rules 在儲存端限制管理員身分、檔案格式與 8 MB 容量。
- 草稿與公開內容分開保存；匿名訪客無法讀取草稿或版本紀錄。
- 圖片使用隨機檔名，降低猜測與覆蓋風險。
- 已加入 Vercel CSP、HSTS、防嵌入、MIME 防護、Referrer Policy、Permissions Policy 與管理頁禁止快取。
- Firebase SDK 鎖定明確版本，外部連結使用 `noopener noreferrer`。
- 管理頁禁止搜尋引擎索引。
- 原始碼與 Git 歷史初步掃描未發現 API 密鑰、密碼或私鑰。
- 已加入 GitHub 及 Vercel 忽略規則，降低私人文件再次被提交或發布的風險。
- Firebase Web 設定已寫入網站；Email/Password 登入、Firestore 正式規則與指定管理員 UID 已完成設定。
- Authentication 已移除誤植帳號，目前只保留指定管理員帳號。
- Firebase Storage 已在 `ASIA-EAST1` 建立並發布正式規則；跨服務規則權限已附加。
- Firebase Authentication 已授權 `5-1bbs.com` 與 `www.5-1bbs.com`。
- CSP 已允許 Firebase 必要的安全 WebSocket 連線（僅限 Google APIs／Firebase），並將 `frame-ancestors` 保留在 Vercel HTTP 回應標頭，避免瀏覽器忽略 `<meta>` 指令的警告。

## 仍需優先處理

### 高風險：舊 GitHub 版本含私人文件

舊遠端版本曾包含合約、SOP、Excel 與場地文件。刪除目前檔案，不代表 Git 歷史中的副本已消失。

建議：

1. 先將舊版本庫改為私人。
2. 使用 `git filter-repo` 徹底移除私人文件並更新遠端，或建立全新的乾淨版本庫。
3. 若文件內含簽名、身分證字號、銀行資料或其他個資，應視同可能已曝光並採取相應處理。
4. 完成後確認 GitHub 原始檔網址已無法下載。

### Firebase Storage 已啟用

Storage 已使用 Standard 類別建立於 `ASIA-EAST1`，並已發布 `firebase/storage.rules`。公開訪客只能讀取 `site-images`；只有 Firestore `admins/{UID}` 名單內的登入帳號可寫入或刪除，且上傳限 JPG、PNG、WebP，單張小於 8 MB。專案使用 Blaze 即付即用方案，建議在 Google Cloud Billing 設定預算通知。

### Firebase Web 設定與私鑰的差別

Firebase Web `firebaseConfig` 會在瀏覽器中使用，本來就是公開識別設定，安全性由 Authentication、Firestore Rules 與 Storage Rules提供。service account JSON 與 private key 則是高敏感伺服器憑證，絕對不能放入 GitHub、Vercel 靜態檔案或傳給他人。

## 結論

新版本已具備資料庫端授權，Firestore、管理員登入、正式網域與 Storage 照片管理均已完成。上線前最大的剩餘風險是舊 GitHub 歷史內的私人文件；建議以全新乾淨版本庫部署，或先徹底清除舊歷史。
